---
name: my-skill
description: Use this skill when the user needs help with a specific workflow. Describe clearly what the skill does, when to use it, and what makes it different from general reasoning.
---

# My Skill

## Purpose

State the job this skill is meant to do.

## When to use

- Use when the user asks for:
- Use when the task involves:
- Do not use when:

## Workflow

1. Gather the minimum context needed.
2. Apply the workflow consistently.
3. Validate the output before finishing.

## Resources

- Read files in `references/` only when needed.
- Use scripts in `scripts/` when the task benefits from deterministic execution.
- Reuse files in `assets/` instead of recreating templates from scratch.
